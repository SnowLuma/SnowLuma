{
  "name": "@snowluma/channel-socket",
  "version": "1.9.2",
  "private": true,
  "type": "module",
  "description": "SnowLuma `socket` channel implementation — placeholder for the future pure-socket runtime (no QQ.exe, no hook). Today registers as a no-op `ChannelAdapter` so the Hub-adapter architecture is exercised end-to-end. Will gain a `SocketClient` long-lived TCP login session when the protocol runtime lands.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./socket-channel": {
      "types": "./src/socket-channel.ts",
      "default": "./src/socket-channel.ts"
    },
    "./socket-adapter": {
      "types": "./src/socket-adapter.ts",
      "default": "./src/socket-adapter.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@snowluma/channel": "workspace:*",
    "@snowluma/common": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "catalog:"
  }
}
